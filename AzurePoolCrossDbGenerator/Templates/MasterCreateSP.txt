-- 0 - master DB name
-- 1 - master table name
-- 2 - mirror DB name

USE [{0}]
DROP PROCEDURE IF EXISTS sp_CopyFromRemote_{1}
GO

CREATE PROCEDURE sp_CopyFromRemote_{1}
(@mirror_key AS UNIQUEIDENTIFIER)
AS
BEGIN
    INSERT INTO {1} SELECT * FROM {2}__{1}_ext WHERE mirror_key = @mirror_key 
END